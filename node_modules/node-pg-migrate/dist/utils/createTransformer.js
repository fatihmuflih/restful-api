"use strict";Object.defineProperty(exports, "__esModule", {value: true});var _ = require('.');
function createTransformer(literal) {
  return (statement, mapping = {}) => Object.keys(mapping).reduce((str, param) => {
    const val = mapping == null ? void 0 : mapping[param];
    return str.replace(
      new RegExp(`{${param}}`, "g"),
      val === void 0 ? "" : typeof val === "string" || typeof val === "object" && val !== null && "name" in val ? literal(val) : String(_.escapeValue.call(void 0, val))
    );
  }, statement);
}


exports.createTransformer = createTransformer;
