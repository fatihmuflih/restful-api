"use strict";Object.defineProperty(exports, "__esModule", {value: true});var _utils = require('../../utils');
var _dropTypeAttribute = require('./dropTypeAttribute');
function addTypeAttribute(mOptions) {
  const _alterAttributeAdd = (typeName, attributeName, attributeType) => {
    const typeStr = _utils.applyType.call(void 0, attributeType, mOptions.typeShorthands).type;
    const typeNameStr = mOptions.literal(typeName);
    const attributeNameStr = mOptions.literal(attributeName);
    return `ALTER TYPE ${typeNameStr} ADD ATTRIBUTE ${attributeNameStr} ${typeStr};`;
  };
  _alterAttributeAdd.reverse = _dropTypeAttribute.dropTypeAttribute.call(void 0, mOptions);
  return _alterAttributeAdd;
}


exports.addTypeAttribute = addTypeAttribute;
