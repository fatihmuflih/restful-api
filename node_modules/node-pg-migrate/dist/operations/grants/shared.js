"use strict";Object.defineProperty(exports, "__esModule", {value: true});function asArray(item) {
  return Array.isArray(item) ? item : [item];
}
function isAllTablesOptions(options) {
  return "schema" in options;
}
function asRolesStr(roles, mOptions) {
  return asArray(roles).map((role) => role === "PUBLIC" ? role : mOptions.literal(role)).join(", ");
}
function asTablesStr(options, mOptions) {
  return isAllTablesOptions(options) ? `ALL TABLES IN SCHEMA ${mOptions.literal(options.schema)}` : asArray(options.tables).map(mOptions.literal).join(", ");
}





exports.asArray = asArray; exports.asRolesStr = asRolesStr; exports.asTablesStr = asTablesStr; exports.isAllTablesOptions = isAllTablesOptions;
