"use strict";Object.defineProperty(exports, "__esModule", {value: true});var _removeFromOperatorFamily = require('./removeFromOperatorFamily');
var _shared = require('./shared');
const addToOperatorFamily = (mOptions) => {
  const method = (operatorFamilyName, indexMethod, operatorList) => {
    const operatorFamilyNameStr = mOptions.literal(operatorFamilyName);
    const operatorListStr = operatorList.map(_shared.operatorMap.call(void 0, mOptions)).join(",\n  ");
    return `ALTER OPERATOR FAMILY ${operatorFamilyNameStr} USING ${indexMethod} ADD
  ${operatorListStr};`;
  };
  method.reverse = _removeFromOperatorFamily.removeFromOperatorFamily.call(void 0, mOptions);
  return method;
};


exports.addToOperatorFamily = addToOperatorFamily;
